import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'GenieCode v3.0 - The AI Collective',
  description: 'Advanced AI-powered development system with 12 specialized agents',
  keywords: 'AI, code generation, autonomous development, multi-agent system',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
